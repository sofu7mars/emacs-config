(menu-bar-mode 0)
(tool-bar-mode 0)
(scroll-bar-mode 0)

(require 'package)
(setq package-enable-at-startup 1)
(add-to-list 'package-archives
    '("melpa" . "https://melpa.org/packages/") t)
(package-initialize)



;(setq default-frame-aliast '((width . 1920) (height . 1080) (menu-bar-lines . 0))

;(elpy-enable)
;(setq
; conda-anaconda-home (expand-file-name "~/anaconda3")
; conda-env-home-directory (expand-file-name "~/anaconda3"))

(global-display-line-numbers-mode)
(setq display-line-numbers-type 'relative)

(custom-set-variables
 ;; custom-set-variables was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 '(custom-safe-themes
   '("d3736d9593c8df05182e64c1bc8fff37987dc8290102c33a08563e5396d25f3e" "e13beeb34b932f309fb2c360a04a460821ca99fe58f69e65557d6c1b10ba18c7" default))
 '(package-selected-packages '(elpy gruber-darker))
 '(display-line-numbers-type 'relative))
 ;'(package-selected-packages '(conda elpy))

;(package-initialize)
;(unless (package-installed-p 'elpy)
  ;(package-refresh-contents)
 ; (package-install 'elpy))
;(elpy-enable)

(setq python-shell-interpreter "/home/vladyslav/myenv/bin/python3")

;(custom-set-faces
 ;; custom-set-faces was added by Custom.
 ;; If you edit it by hand, you could mess it up, so be careful.
 ;; Your init file should contain only one such instance.
 ;; If there is more than one, they won't work right.
 ; )


(add-to-list 'custom-theme-load-path "~/.emacs.d/gruber-darker-theme.el")
(load-theme 'gruber-darker t)


